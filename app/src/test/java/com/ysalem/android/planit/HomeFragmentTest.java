package com.ysalem.android.planit;

import android.content.Context;
import android.content.res.Resources;
import android.util.DisplayMetrics;

import com.ysalem.android.planit.ui.HomeFragment;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class HomeFragmentTest {



    @Test
    public void calculateNoOfColumns_SmallScreens() {

//        int expectedResult = 2;
//        DisplayMetrics displayMetrics = mock(DisplayMetrics.class);
//        Resources mockResources = mock(Resources.class);
//        when(mockResources.getDisplayMetrics()).thenReturn(displayMetrics);
//        Context mockContext = mock(Context.class);
//        when(mockContext.getResources()).thenReturn(mockResources);
//
//        displayMetrics.widthPixels = 1080;
//        displayMetrics.density = (float) 2.5;
//        int result = HomeFragment.calculateNoOfColumns(mockContext);
//        assertEquals(expectedResult, result);

    }

    @Test
    public void calculateNoOfColumns_LargeScreens() {

//        int expectedResult = 6;
//        DisplayMetrics displayMetrics = mock(DisplayMetrics.class);
//        Resources mockResources = mock(Resources.class);
//        when(mockResources.getDisplayMetrics()).thenReturn(displayMetrics);
//        Context mockContext = mock(Context.class);
//        when(mockContext.getResources()).thenReturn(mockResources);
//
//        displayMetrics.widthPixels = 2736;
//        displayMetrics.density = 2;
//        int result = HomeFragment.calculateNoOfColumns(mockContext);
//        assertEquals(expectedResult, result);

    }
}
